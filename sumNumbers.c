#include <conio.h>
#include <stdio.h>
void main(){
	int a,b,sum;
	printf("Enter the two numbers to be added:");
	scanf("%d %d",&a,&b);
	sum=a+b;
	printf("The sum of the two numbers is:%d",sum);
	getch();
}
